/**
 * Difference in pixels between resting and animated x translation at which the page is near settled and it's adjacent
 * offscreen page can be rendered and mounted without sacrificing animation smoothness
 */
export const X_DIFF_SETTLE_THRESHOLD = 1;
